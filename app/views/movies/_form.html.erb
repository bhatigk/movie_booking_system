<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-4">
    <%= form.label :poster, style: "display: block", class: "form-label" %>
    <%= form.file_field :poster, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.label :genre, style: "display: block" %>
    <%= form.text_field :genre, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.submit "Add Movie", class: "btn btn-primary" %>
  </div>
<% end %>
